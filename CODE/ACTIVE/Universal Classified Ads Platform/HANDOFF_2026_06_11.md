# HANDOFF — 2026-06-11 Classified Ads Platform

## What's Done

### Plugin Deployed on cifn.eu (LIVE)
- **Plugin**: `classified-ads-bridge` activated on cifn.eu WordPress
- **Pages**: 
  - https://cifn.eu/post-ad/ (ID 126, shortcode `[classified_ads_form]`)
  - https://cifn.eu/my-ads/ (ID 128, shortcode `[my_classified_ads]`)
- **Plugin settings** (configured in WP options):
  - `cab_api_url` = `https://api.cifn.eu`
  - `cab_price_cents` = **0** (free mode, no Stripe needed for testing)
  - `cab_fb_app_id` = empty (no FB app)
  - `cab_stripe_pk` = empty
- **Native login/register**: Plugin now supports email+password registration as fallback to FB login. Users can create accounts and post ads without any Facebook App.

### WP Auth Working
- **WP user**: `apaminerala@yahoo.com` (username "skdde", role: administrator)
- **WP app password**: `9yS6yIqd18IbIA9LXve5mj6A` (generated via cPanel PHP script)
- **cPanel API token**: `K9ATCMHPKVSKUV2M97447JLY45EH29KQ` (user: loaiidil)
- **cPanel API format**: Header `Authorization: cpanel loaiidil:TOKEN` (NOT Basic auth)
- **cPanel Fileman**: `save_file_content` works for writing files, `get_file_content` for reading. Uses `dir` + `file` params.

### Backend Code (Local, Ready to Deploy)
- **Project**: `D:\MEMORY\CODE\ACTIVE\Universal Classified Ads Platform\backend\`
- **Import check**: PASSING
- **42 pytest tests**: PASSING
- **Key .env settings**:
  - `WP_SITE_URL=https://cifn.eu`
  - `WP_ENABLED=false` (flip to true after backend is reachable)
  - `WP_APP_PASSWORD=` (fill: `9yS6yIqd18IbIA9LXve5mj6A`)
  - `WP_USER=admin` (actually `apaminerala@yahoo.com` for REST API)
  - Stripe fields: empty (sandbox mode)
  - `POSTHOG_ENABLED=false`

### Plugin Code Changes (This Session)
- **`classified-ads-bridge.php`**: Added `cab_ajax_native_register` and `cab_ajax_native_login` AJAX handlers. Login form now shows email+password fields alongside FB button (FB hidden when no app ID). Register form with toggle. Price=0 shows "Post Ad" button (not "Pay & Post").
- **`js/app.js`**: Added `nativeLogin()`, `nativeRegister()`, `sendToWP()` unified AJAX function. Toggle between login/register forms. Payment flow: if price=0 → free submit, if sandbox → auto-confirm, if real Stripe → redirect.

---

## What's NOT Done (Blockers)

### 1. FastAPI Backend Not Reachable at api.cifn.eu
- `https://api.cifn.eu/` returns 000 (unreachable)
- **Need**: Deploy FastAPI somewhere accessible + configure api.cifn.eu DNS/proxy to point to it
- **Options**:
  - (a) **raspibig** — Port 8004 available, PostgreSQL DB `classified_ads` created, venv started but deps incomplete. Need: finish pip install, create systemd service, set up Caddy proxy for api.cifn.eu → localhost:8004
  - (b) **Laptop** — Run `uvicorn app.main:app --port 8000` locally, use Cloudflare tunnel / ngrok to expose as api.cifn.eu
  - (c) **A2 Hosting** — Run FastAPI on A2 via cPanel (Python app via Passenger/Phusion)
  - **Quickest test**: Option (b) — laptop + tunnel, since FastAPI already works locally

### 2. Facebook App (Optional)
- **No FB_APP_ID or FB_APP_SECRET found anywhere** in the entire D:\MEMORY codebase
- Only found: `FB_ACCESS_TOKEN` (page posting token) in `farmworkers_facebook_poster.py`
- **Current state**: Native email+password login works in the WP plugin, no FB needed for testing
- **To add FB later**: Create app at developers.facebook.com → get App ID + Secret → enter in WP Classified Ads settings page
- **Alternative**: Install "Super Socializer" or "Nextend Social Login" WP plugin that provides its own shared FB app

### 3. raspibig Deployment (Partial)
- Files copied to `/opt/ACTIVE/classified-ads/`
- PostgreSQL DB `classified_ads` created (owned by postgres)
- Port 8004 chosen (8000=interjob FastAPI, 8001=email-api, 8002-8003=other, 7740=api.cifn.eu current)
- **Stuck at**: pip install in venv (deps not fully installed)
- **Caddy**: Need to add `api.cifn.eu { reverse_proxy localhost:8004 }` to Caddyfile

---

## How to Test (Once Backend is Up)

### 1. Start FastAPI backend
```bash
# On laptop (quick test):
cd "D:\MEMORY\CODE\ACTIVE\Universal Classified Ads Platform\backend"
python -m uvicorn app.main:app --port 8000 --reload

# Or on raspibig:
plink -batch -pw 'bucare' tudor@192.168.100.21 "cd /opt/ACTIVE/classified-ads && source venv/bin/activate && uvicorn app.main:app --port 8004"
```

### 2. Make api.cifn.eu point to it
- If laptop: set up Cloudflare tunnel or ngrok
- If raspibig: add Caddy config + DNS A record

### 3. Test the full flow
1. Visit https://cifn.eu/post-ad/
2. Click "Create account" → fill name/email/password → "Create Account & Login"
3. Fill ad form (title, description, category, location) → "Post Ad"
4. Ad created as draft → auto-submitted (free mode, price=0)
5. As moderator: visit FastAPI /moderation/ page → approve → publish
6. Published ad pushed to cifn.eu as WP post (if WP_ENABLED=true)

### 4. Enable Stripe (when ready)
- Set `cab_price_cents = 500` ($5.00) in WP Classified Ads settings
- Add `STRIPE_SECRET_KEY` + `STRIPE_PUBLISHABLE_KEY` to backend .env
- Set `cab_stripe_pk` in WP settings

---

## Key Files

| File | Purpose |
|------|---------|
| `backend/.env` | Backend config (WP_SITE_URL, Stripe, PostHog) |
| `backend/app/main.py` | FastAPI app (routes, startup seed, page serving) |
| `backend/app/services/wordpress.py` | Push published ads to WP as posts |
| `backend/app/api/routes/payments.py` | Stripe + sandbox checkout |
| `wordpress-plugin/classified-ads-bridge/classified-ads-bridge.php` | WP plugin (FB + native login, shortcodes, AJAX) |
| `wordpress-plugin/classified-ads-bridge/js/app.js` | Frontend JS (auth, ad form, checkout, my-ads) |
| `deploy_cifn_eu.py` | Deployment script (cPanel subdomain + plugin + Caddy) |
| `backend/tests/` | 42 pytest tests (auth, categories, workflow) |

## Credential Reference

| Service | Key | Value |
|---------|-----|-------|
| cPanel | User | loaiidil |
| cPanel | API Token | K9ATCMHPKVSKUV2M97447JLY45EH29KQ |
| WP cifn.eu | User | apaminerala@yahoo.com |
| WP cifn.eu | App Password | 9yS6yIqd18IbIA9LXve5mj6A |
| raspibig SSH | User/Pass | tudor / bucare |
| raspibig SSH | Plink | `& 'C:\Program Files\PuTTY\plink.exe' -batch -pw 'bucare' tudor@192.168.100.21` |
